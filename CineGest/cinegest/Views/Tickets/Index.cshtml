@model IEnumerable<CineGest.Models.Tickets>

@{
    ViewData["Title"] = "Bilhetes";
}

<h1>Bilhetes</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Session.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seat)
            </th>
            <th>
                Opções
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-controller="Sessions" asp-action="Details" asp-route-id="@item.Session.Id">
                        @Html.DisplayFor(modelItem => item.Session.Id)
                    </a>
                </td>
                <td>
                    <a asp-controller="Users" asp-action="Details" asp-route-id="@item.User.Id">
                        @Html.DisplayFor(modelItem => item.User.Email)
                    </a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Seat)
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id" title="Detalhes"><i class="fas fa-info mx-2"></i></a>
                    <a asp-action="Delete" asp-route-id="@item.Id" title="Apagar"><i class="fas fa-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
