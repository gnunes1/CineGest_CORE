@model IEnumerable<CineGest.Models.Sessions>

@{
    ViewData["Title"] = "Sessões";
}

<h1>Sessões</h1>

<p>
    <a asp-action="Create">Criar novo registo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CinemaFK)

            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieFK)

            </th>
            <th>
                @Html.DisplayNameFor(model => model.Start)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.End)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Occupated_seats)
            </th>
            <th>
                Opções
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-controller="Cinemas" asp-action="Details" asp-route-id=@item.Cinema.Id>
                        @Html.DisplayFor(modelItem => item.Cinema.Name)
                    </a>
                </td>
                <td>
                    <a asp-controller="Movies" asp-action="Details" asp-route-id="@item.Movie.Id">
                        @Html.DisplayFor(modelItem => item.Movie.Name)
                    </a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Start)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.End)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Occupated_seats)
                </td>
                <td>
                    <a asp-action="Delete" asp-route-id="@item.Id" title="Apagar"><i class="fas fa-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
